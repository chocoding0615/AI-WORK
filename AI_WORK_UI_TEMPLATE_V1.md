# AI-WORK UI TEMPLATE V1

이 문서는 AI-WORK #001~#005에 적용한 실무형 분석 화면의 고정 기준이다. #006 이후에는 이 문서와 완성된 화면의 정보 순서와 상호작용을 그대로 복제하고, 도구별 입력 스키마·계산 결과·업무 문안만 교체한다.

## 공통 화면 순서

1. PRODUCT HEADER
2. INPUT
3. RESULT HEADER
4. KPI STRIP
5. FIRST RESPONSE / TOP 5
6. ALL RESULTS
7. DETAIL MODAL

## 헤더 구조

전체 너비의 진한 네이비 제품 헤더에 번호 배지(`01`~`05`), `AI-WORK #00N`, 도구명, 한 줄 설명을 왼쪽에 둔다. 오른쪽에는 분석 전 `분석 대기`, 분석 후 초록색 상태점과 `N건 분석 완료`를 표시한다. 헤더 하단에는 홈과 #001~#005 이동 경로를 제공한다.

## INPUT 구조

흰색 입력 영역의 상단에 `INPUT`, 도구별 입력 제목, 오른쪽 `샘플 양식 다운로드`를 배치한다. 본문에는 파일 선택 또는 텍스트 입력, 현재 파일명/입력 상태, 필수 열·조건, 진한 네이비 분석 버튼을 둔다. CSV 도구는 클릭 선택과 드래그앤드롭을 모두 지원하고, #004는 큰 텍스트 영역을 사용한다. 분석 후에도 입력 영역은 유지한다.

## RESULT 구조

결과 영역은 연회색 배경 위에 `RESULT`와 도구별 결과 제목을 두고, 오른쪽에 전체 요약 복사 및 결과 파일 다운로드를 배치한다. 분석 전에는 결과 영역을 숨기고, 결과가 없는 경우에는 명확한 빈 상태를 표시한다.

## KPI 규칙

KPI는 같은 높이의 가로 연결 타일로 표시한다. 핵심 숫자는 크게, 설명은 짧게 표시하며 위험·주의 수치에만 절제된 상태 색상을 사용한다. 모바일에서는 2열 또는 1열로 전환한다.

## TOP 5 규칙

사용자가 먼저 처리할 다섯 건 또는 다섯 개의 개선 대상을 보여준다. 데스크톱에서는 동일 너비의 가로 카드, 모바일에서는 세로 카드로 배치한다. 순위, 상태/등급, 핵심 식별자, 날짜·기간, 한 줄 핵심 내용만 노출하고 상세 정보는 모달에서 제공한다.

## 전체 결과 표 규칙

영어 보조 라벨과 한국어 제목을 함께 표시하고 우측 필터를 둔다. 표에는 업무에 필요한 핵심 열, 상태 배지, 판단 근거·다음 행동 요약, 일관된 상세 액션 버튼을 둔다. 모바일에서는 표 영역만 가로 스크롤한다.

## 상세 모달 규칙

TOP 5 카드와 전체 결과의 상세 버튼은 식별자·상태, 원문/원본, 코드 계산 근거, AI 분류·해석, 다음 행동, 실무 문안, 개별 문안 복사를 포함한 상세 모달을 연다. 모달은 ESC, 배경 클릭, 닫기 버튼으로 닫히며 열릴 때 포커스를 모달로 이동하고 닫을 때 호출 버튼으로 포커스를 돌려준다. 모달 뒤 페이지 스크롤을 잠그고, 작은 화면에서는 모달 내부만 스크롤한다. 사용자 입력은 DOM `textContent`/안전한 DOM 생성으로 표시한다.

## 복사·다운로드 위치

- INPUT 오른쪽 상단: 샘플 양식 다운로드
- RESULT 오른쪽 상단: 전체 요약 복사, 결과 CSV/TXT 다운로드
- DETAIL MODAL 하단: 개별 문안 복사

## 상태·오류·빈 결과 규칙

분석 전 결과 영역은 숨긴다. 분석 중에는 분석 버튼을 비활성화하고 상태를 알려준다. 오류는 입력 영역 가까이에 사용자용 문장으로 표시하며 내부 스택이나 AI 응답 원문은 노출하지 않는다. 결과가 0건이어도 성공 상태와 `발견되지 않았습니다` 빈 상태를 표시한다. 일부 행이 제외되면 경고를 결과와 함께 표시한다.

## 모바일 규칙

390px 폭에서 문서 전체 가로 overflow가 없어야 한다. 헤더·KPI·TOP 5는 화면에 맞게 줄바꿈하며, 표만 내부 가로 스크롤한다. 버튼은 잘리지 않고, 모달 본문만 세로 스크롤한다.

## 콘텐츠 폭 규칙

데스크톱의 PRODUCT HEADER 내부, INPUT, RESULT는 동일한 콘텐츠 기준선을 사용한다. 콘텐츠 폭은 `width: calc(100% - 48px)`로 두고 최대 `1760px`에서 멈춘다. 따라서 1920px 화면에서는 좌우 약 80px, 1440px 이하 데스크톱에서는 좌우 24px을 사용한다. 768px 이하에서는 좌우 16px(`width: calc(100% - 32px)`)으로 전환한다. RESULT 내부에서 중첩 컨테이너가 폭을 다시 제한하지 않도록 KPI·TOP 5·전체 결과 표는 이 메인 폭을 그대로 사용한다.

## 접근성 규칙

버튼과 링크는 키보드로 접근 가능해야 하며, 상태 변경에는 `aria-live`를 사용한다. 모달은 `role="dialog"`, `aria-modal="true"`, 제목 연결을 사용한다. ESC·배경·닫기 버튼을 지원하고 포커스를 복귀한다. 사용자 값은 HTML 문자열 삽입 대신 안전한 DOM API로 렌더링한다.

## #006 이후 변경 가능한 영역

도구 번호, 도구명·설명, 입력 스키마, KPI 이름과 계산, TOP 5 업무 내용, 표 열, 모달 업무 정보, 복사 문안, 결과 파일 형식만 변경한다.

## #006 이후 변경하면 안 되는 영역

전체 정보 순서, 제품 헤더, 입력 영역 위치, 결과 액션 위치, KPI 표현, TOP 5 위치, 전체 결과 위치, 상세 모달 인터랙션, 버튼 계층, 색상·간격·테두리, 반응형 기준은 고정한다.
